---
interface Props {
  currentPage?: string;
}
const { currentPage = '' } = Astro.props;

const navLinks = [
  { href: '/#features', label: 'Features' },
  { href: '/#how-it-works', label: 'How It Works' },
  { href: '/pricing', label: 'Pricing' },
  { href: '/about', label: 'About' },
  { href: '/demo', label: 'Demo' },
  { href: '/blog', label: 'Blog' },
  { href: '/case-studies', label: 'Case Studies' },
  { href: '/contact', label: 'Contact' },
];
---

<header class="site-header" id="site-header">
  <nav class="container nav-container" aria-label="Main navigation">
    <a href="/" class="logo">
      <img src="/logo.png" alt="Alure.ai" class="logo-img" width="120" height="32" onerror="this.style.display='none';this.nextElementSibling?.classList.add('show')" />
      <span class="logo-text">Alure.ai</span>
    </a>

    <button
      class="mobile-menu-toggle"
      aria-label="Toggle navigation menu"
      aria-expanded="false"
      aria-controls="nav-menu"
    >
      <span></span>
      <span></span>
      <span></span>
    </button>

    <div class="mobile-menu-overlay" id="menu-overlay" aria-hidden="true"></div>
    <ul class="nav-links" id="nav-menu" role="menubar">
      {navLinks.map(link => (
        <li role="none">
          <a
            href={link.href}
            role="menuitem"
            class:list={[{ active: currentPage === link.label.toLowerCase() }]}
          >
            {link.label}
          </a>
        </li>
      ))}
      <li role="none">
        <a href="https://app.alure.ai" role="menuitem">Login</a>
      </li>
      <li role="none">
        <a href="https://app.alure.ai" class="btn btn-primary btn-nav" role="menuitem">Sign Up</a>
      </li>
    </ul>
  </nav>
</header>

<a href="#main-content" class="skip-link">Skip to main content</a>

<script is:inline>
  document.addEventListener('DOMContentLoaded', function() {
    const toggle = document.querySelector('.mobile-menu-toggle');
    const menu = document.getElementById('nav-menu');
    if (!toggle || !menu) return;

    var overlay = document.getElementById('menu-overlay');

    function closeMenu() {
      toggle.setAttribute('aria-expanded', 'false');
      toggle.classList.remove('active');
      menu.classList.remove('open');
      overlay.classList.remove('open');
      document.body.classList.remove('menu-open');
    }

    toggle.addEventListener('click', function() {
      var isOpen = toggle.getAttribute('aria-expanded') === 'true';
      toggle.setAttribute('aria-expanded', String(!isOpen));
      toggle.classList.toggle('active');
      menu.classList.toggle('open');
      overlay.classList.toggle('open');
      overlay.setAttribute('aria-hidden', String(isOpen));
      document.body.classList.toggle('menu-open');
    });

    overlay.addEventListener('click', closeMenu);

    document.querySelectorAll('.nav-links a').forEach(function(link) {
      link.addEventListener('click', closeMenu);
    });
  });
</script>
